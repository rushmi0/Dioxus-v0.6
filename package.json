{
  "name": "rust-webassembly",
  "version": "0.1.1",
  "description": "",
  "main": "tailwind.config.js",
  "scripts": {
    "web": "dx serve --platform web --hot-reload true",
    "build": "dx build --release",
    "clean": "dx clean"
  },
  "private": true,
  "devDependencies": {
    "daisyui": "^4.12.22"
  }
}
